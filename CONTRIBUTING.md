# 一起来填

这个项目最缺的不是代码，是**会说方言的人**。

## 挑错（门槛最低，价值最高）

站上所有台词都是 AI 写的，包括那几句德州话——作者不是德州人。**你听着别扭，八成就是错的。**

开一个 issue，标题写方言名，正文告诉我们：

- 哪一拍（比如「深夜回家 · 第 5 拍」）
- 现在写的是什么
- 你们那儿怎么说
- 你是哪儿人（精确到县市就行，愿意写到乡镇更好）

不用讲道理，不用引证，**你说不这么说，那就是不这么说**。每一条采纳后，那份演绎的可信度会从「AI 生成 · 未经母语者校对」升成「AI 生成 · 已经母语者校对」，页面上的红色警示标签会变成土黄色。

## 加一份方言演绎

照 `src/data/performances/late-night.jilu.yaml` 的结构写一份新文件，命名 `{场景id}.{方言id}.yaml`。

```yaml
id: late-night.你的方言id
sceneId: late-night
dialectId: 你的方言id        # 必须已存在于 src/data/dialects.yaml
source: tts                  # 真人录音写 human
verification: unverified     # 你自己是母语者就写 native_verified
lines:
  - beatId: b1
    textDialect: 方言原话
    textMandarin: 普通话直译（尽量直，不要意译成漂亮的书面语）
    note: 解释这句里的地方说法，比如「多咱＝什么时候」
```

**六拍必须齐全**，缺一拍构建就会失败——这是故意的，横向对比页靠逐拍对齐工作，少一拍会让整列错位。

### 三条硬规矩

**一、只写你自己会说的方言。** 这个项目宁可少一个方言，也不要一份查着资料拼出来的塑料方言。母语者一眼看穿，然后整个站的可信度就没了。

**二、方言 id 必须挂在正确的层级上。** 如果你要新增方言节点，先在 `src/data/dialects.yaml` 里按《中国语言地图集》的五层分区（大区/区/片/小片/点）放对位置。**不要为了省事造一个笼统的省级标签**——「山东话」「江苏话」这种节点在这个项目里不存在，因为它们跨多个官话区，合并等于说假话。

**三、AI 合成的演绎不许挂到「点」或「小片」级。** 模型给不出县域口音。真人录音（`source: human`）不受此限制——人开口，话就带着他村的坐标。

写完跑一遍：

```bash
pnpm test && pnpm check && pnpm build
```

`validateContent()` 会检查漏拍、多拍、层级违规、枚举写错。构建能过，数据就是干净的。

## 加一副场景骨架

在 `src/data/scenes/` 新建 YAML。**骨架只描述情节意图，绝不写具体台词**——台词是各地自己填的肉，骨架是所有方言共用的骨。

```yaml
id: market
title: 菜市场砍价
situation: 一斤排骨要三十二，你觉得贵了。
beats:
  - id: b1
    order: 1
    intent: 顾客嫌贵
    speakerRole: buyer        # 见下方说明
```

好场景的标准是**全国人民都经历过**：菜市场砍价、亲戚劝婚、跟爸妈解释为什么还不买房、小孩考砸了回家。越普遍，横向对比越有意思——因为读者完全知道该说什么，才能听出别人说得多不一样。

留一个空拍（`speakerRole: none`）当结尾往往很有效，沉默在所有方言里都一样。

**角色由场景自己声明**，不再是写死的枚举。每个角色要给一档声色（`adult_female` / `adult_male` / `young_female` / `young_male`），各方言各自去找自己有的、对得上这一档的真实方言音色：

```yaml
roles:
  seller:
    name: 摊主
    voice: adult_female
    mood: 用不耐烦、嫌你烦的口气说      # TTS 情绪指令，可留空
  buyer:
    name: 顾客
    voice: adult_male
    mood: 用讲价讲到有点上头的口气说
```

一场戏里**至少要有两个不同声色的角色**——都用同一档，两个人会共用一副嗓子，听起来像自问自答。`none` 是空拍的保留值，不能拿来当角色名。

## 真人录音（不用碰 GitHub）

这是最有价值的一种贡献，而且**不需要你会用 git**：直接去 <https://geshuo.pages.dev/record/>，网页上录，录完点上传。

- 桌面浏览器可以现场录；手机上如果录不了，用「选个文件」把语音备忘录里已有的录音传上来，一样。
- 填清楚你是哪儿人——**越细越好**，乡镇比县好，县比市好。你的录音会挂到你写的那个点上。
- 录音只上传、不公开。**上传接口只收不发，站上没有任何读取录音的路由**——你录的是自己的声音，不该挂在公网上任人下载。审核在维护者本地做（`pnpm recordings:pull` 取回，本地审听台里逐条听）。听过、确认是本人本地口音，才会替换掉那条 AI 版，并把那份演绎标成「真人录音」。

**别照着现有的方言版本念。** 照念会把那个版本的措辞带进来，那就不是你的话了。看这一拍要表达什么意思，用你们那儿真会说的话说一遍就行——包括脏字，见下。

## 关于脏话

这个项目**不删脏话**。理由写在 [脏话也是方言](https://geshuo.pages.dev/swearing/) 那一页：骂人话是方言里最不容易被普通话洗掉的部分，删掉它剩下的不是方言，是普通话换了个腔调。那一页还有一场公开投票，欢迎表态。

写台词或者录音时：**骂人的粗口照写照录**，这是真实的一部分；但**针对族群、性别、地域、残障的仇恨话不收**——那不是哪个地方的方言特色，不需要这个站来替它保存。

## 提交前

- 只提交你改动的文件，不要带上 `public/audio/`（音频不进仓库，见 README）
- commit message 用中文，说清楚改了哪个方言的哪一拍
- 你贡献的方言数据将以 [CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/deed.zh) 发布，代码以 MIT 发布

方言是所有说它的人的。你把家乡话写下来，是让它多活一会儿。
